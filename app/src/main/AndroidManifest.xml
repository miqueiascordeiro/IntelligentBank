<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miqueiascordeiro.intelligentbank">

    <application
        android:allowBackup="true"
        android:icon="@drawable/projeto"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".apresentacaoApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".telaprincipal" />
        <activity android:name=".deposito" />
        <activity android:name=".MainActivity" />
        <activity android:name=".saque" />
        <activity android:name=".relatorio" />
        <activity android:name=".cadastro"></activity>
    </application>

</manifest>